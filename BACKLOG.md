^1# Backlog

Personal planning overview. User feedback and bug reports: [GitHub Issues](https://github.com/esc1899/wealth_management/issues)

> 📦 Abgeschlossene Einträge ausgelagert → [BACKLOG_ARCHIVE.md](BACKLOG_ARCHIVE.md)

## Legend
- `[P1]` High / `[P2]` Medium / `[P3]` Low
- `[BUG]` Bug / `[FEAT]` Feature / `[IMPR]` Improvement / `[DEBT]` Technical Debt

---

## In Progress

(None)

---

## Bekannte Probleme (nicht dringend)

| ID | Entdeckt | Beschreibung |
|---|---|---|
| NOTE-1 | 2026-06-06 | ~~**Tavily Monats-Limit erschöpft**~~ — Plan-Limit erhöht (2026-06-07). Verbrauch zusätzlich optimiert: search_depth=basic (alle außer FA), client-side max_uses-Enforcement, fehlende Limits nachgerüstet, NewsAgent-Formel gekappt. ✅ ERLEDIGT |
| NOTE-2 | 2026-06-06 | **SCANFL delisted/nicht gefunden** — yfinance liefert 404 für Symbol SCANFL. Position prüfen: Ticker noch aktuell? Ggf. aus Portfolio/Watchlist entfernen oder Ticker korrigieren. |

---

## Planned

### Features

| ID | Priority | Type | Description | Status | Session |
|---|---|---|---|---|---|
| FEAT-33 | P3 | [FEAT] Cowork Outbox — Rückkanal | ~~Ursprünglicher Ansatz (file-based) obsolet.~~ **Ersetzt durch FEAT-50 + FEAT-51.** | ↗ FEAT-50/51 | |
| FEAT-62 | P3 | [FEAT] Verdict Hindsight v3 — on-demand KI-Deutung | **Kontext (2026-06-16):** v1/v2 sind deterministisch + statischer Methodik-Expander (stabile Caveats: Momentum/Horizont-Bias, Linsen, Autokorrelation, Survivorship). Offen bleibt nur die **laufspezifische** Deutung („in diesem Lauf sticht X heraus") — die rechtfertigt ein LLM, lohnt aber erst, wenn die **3M/6M-Spalten gefüllt** sind. **Terminiert: ~September 2026** (älteste April-Urteile bei +6M). **Design-Constraints (kritisch):** (1) **Privacy** — LLM sieht **nur den aggregierten Report** (Agent × Verdict × Horizont → Median/n, keine Ticker, keine Einzelpositionen/Scope-Zuordnung). So aggregiert public-safe → darf Cloud sein; auf Rohdaten müsste es Ollama sein. FEAT-60-Guard muss den neuen Agent klassifizieren. (2) **Anti-Overclaim** — Systemprompt nennt n explizit, verbietet Behauptungen bei kleinem/autokorreliertem n, hält „Tagebuch, keine Trefferquote"-Rahmung. (3) **on-demand Button**, kein Auto-Run (Kosten + die deterministische Rahmung soll primär bleiben). (4) Datums-/Reife-Kontext (welche Horizonte reif) muss in den Prompt. | 🔲 TODO | |
| FEAT-68 | P2 | [FEAT] Accumulation Indicator pro Position (Phase B von FEAT-67) | **Kontext (2026-06-19):** Der Accumulation Score als **deterministischer Indikator pro Position** über **Portfolio UND Watchlist** — präzisiert den vagen „Score auf der Dividenden-Seite" aus FEAT-67. **Bewusst kein LLM-Agent:** alle Inputs sind Zahlen/Codes → reine `core/`-Funktion (wie `composition_drift`/`macro_context`), kein Batch-Job, keine Token-Kosten, läuft sofort auf allen Positionen (= Screening), kein Privacy-/Injection-Vektor, braucht **keine** FEAT-60-Bucket-Klassifikation (kein Agent; sieht Stückzahlen, sendet nichts — 🔢-Logik). **Eriks Zerlegung (ex-ante vs ex-post):** **(B1) Akkumulations-*Erwartung* — JETZT baubar, keine neue Datenquelle:** Engine = aktuelle Rendite (`dividend_data.yield_pct` / Valuations) gegated durch die bereits gespeicherten **LLM-Verdicts** als Qualitäts-/Survival-Proxy (Storychecker = These intakt?, FA = Bewertung/Qualität, CG = Gap, CA = Watchlist-Qualität) aus `position_analyses`. Der Indikator *liest* nur Verdict-Codes + Rendite, ruft selbst kein LLM → bleibt deterministisch. **(B2) Akkumulations-*Überprüfung* — reift mit der Historie:** hat die Ratsche real gegriffen? = realisierter Anteilszuwachs + Forward-Income-Wachstum (Phase-A-Funktionen `share_count_series`/`portfolio_income_series`) + DGR aus `ticker.dividends` + optionale yfinance-Fundamentals (FCF-Payout, ROIC, Leverage via bereits genutztem `ticker.info`). Das ist der gemessene **Thesis-Drift-Detektor**. **Score = B1 × B2** (Überprüfung zeigt „baut sich auf", bis genug Snapshots da sind → graceful degradation). **Transparenz (Designprinzip):** zwei sichtbare Achsen, jede Komponente mit Rohwert + Ampel + Schwelle, multiplikative Gates ⇒ bindende Komponente immer benennbar („Was bremst?"); fehlende Felder = „n/a", Score nennt die Lücke. **Trap-Detektor:** hohe Rendite + schwaches Survival-Verdict = `fallen_verdacht` statt „billig". **Andock-Punkte (alle vorhanden):** neue Spalte in Portfolio-Checker- + Watchlist-Cockpit-Status-Matrix (`fmt_verdict_matrix` + neuer `VERDICT_CONFIGS`-Eintrag in `core/ui/verdicts.py`), Karte im Position Dashboard (FEAT-25) neben SC/CG/FA, sortierbar = Watchlist-Entry-Screen. Verdict-Codes deutsch: `akkumulieren`/`halten`/`prüfen`/`fallen_verdacht`/`ungeeignet`. **Arbeitsteilung:** LLM-Checker = *qualitatives* Survival-Narrativ, dieser Indikator = *quantitative* Income-Mathematik — nebeneinander in derselben Matrix; löst den früher offenen „Survival-Probability"-Soft-Spot. **Reihenfolge:** B1 zuerst (sofortiger Nutzen), B2 inkrementell. Datenrealität beachten: yfinance-Fundamentals lückenhaft → B2-Fundamentalteil optional/degradierend. **B1 umgesetzt (2026-06-19):** `core/accumulation.py` (pure, deterministisch) — `compute_accumulation(yield, story, fa)` mit Bänder (Rendite 🟢≥2.5%/🟡≥1.2%/🔴) + Verdict-Regel (akkumulieren/halten/prüfen/fallen_verdacht/ungeeignet) + transparenter Komponenten-Breakdown + `binding` („Was bremst?"); `accumulation_for_position` als Page-Helper. `VERDICT_CONFIGS["accumulation"]` + `accumulation_matrix_cell` in `core/ui/verdicts.py`. Neue Spalte in Portfolio-Checker- (`portfolio_story.py`) + Watchlist-Cockpit-Matrix (`watchlist_checker.py`); „📈 Akkumulation"-Sektion mit Badge + Komponententabelle + binding-Caption im Position Dashboard. i18n de/en. 24 neue Tests (jeder Verdict-Pfad + Bänder-Grenzen + graceful None), AppTest-Render der 3 Seiten clean. **Bugfix (gleicher Tag, vom User gefunden):** Rendite wurde roh aus `dividend_data` gelesen (yield_pct dort oft None, z.B. ALV.DE → „—" rot trotz 4,5 % Dividende). Quelle auf die Valuation-Schicht umgestellt (`get_portfolio_valuation().dividend_yield_pct`, mit Override + Cross-Currency); `accumulation_for_position` nimmt jetzt `yield_map` statt `div_records`. ALV.DE → 4,5 % 🟢 / akkumulieren. 1231 grün. **B2 (Überprüfung) bleibt offen.** | 🟡 B1 DONE | 2026-06-19 |
| FEAT-69 | P2 | [FEAT] Ticker beim Anlegen einer Watchlist-Position erfassen + validieren | **Kontext (2026-06-19):** Beim Accumulation-Yield-Debugging fielen mehrere Positionen ohne Markt-/Dividendendaten auf, weil ihr **Ticker fehlt oder unvollständig** ist (kein Börsensuffix → yfinance-404): `FMG` (statt `FMG.AX`), `4063`/`6861` (japanische Titel ohne `.T`), `KMAR.OL`. Ohne validen Ticker liefert yfinance weder Kurs noch Dividende → die Position ist in jeder datengetriebenen Auswertung blind (Yield „—", kein Snapshot-Beitrag, kein TagesP&L). **Root cause:** der Watchlist-Anlage-Flow erzwingt/prüft beim Anlegen keinen funktionierenden Ticker. **Idee:** Im Add-Formular (Positionen-Seite + Cowork-Inbox-Import) Ticker als Feld führen und beim Speichern gegen yfinance gegenprüfen (1 Light-Fetch: existiert ein Kurs?) — bei 404 Warnung + Vorschlag (Suffix-Heuristik `.AX`/`.T`/`.OL`/…), aber nicht hart blockieren (manuelle Assets wie Konten/Immobilien haben bewusst keinen Ticker → asset_class-abhängig). **Verwandt:** NOTE-2 (SCANFL delisted), FEAT-68 (Indikator braucht Yield). **Aufwand:** klein–mittel; berührt Add-Form-Validierung + evtl. eine `validate_ticker`-Hilfsfunktion (yfinance fast_info). **Umgesetzt (2026-06-20):** `core/ticker_validator.py` (`validate_ticker` via yfinance `fast_info.last_price`, fail-safe → False bei Netz-/Parse-Fehler; `COMMON_SUFFIXES`-Heuristik für Vorschläge). Add-Form in `positionen.py` prüft beim Speichern **non-blocking** (nur bei `cfg.auto_fetch`) — bei 404 Warnung mit Suffix-Vorschlägen via `_pos_ticker_warning` session_state, Position wird trotzdem gespeichert. i18n de/en (`validating_ticker`, `ticker_not_found`). 8 neue Tests. Nebenher: dividend_calendar zeigt jetzt **alle** Portfolio-Positionen im Verlaufs-Chart (auch ohne Dividend-Config, mit „keine Verlaufsdaten"-Hinweis) + Punkt-Dedup pro Datum + dd.mm.yyyy-Achsen; statistics-Monats-Chart mit deutschen Monatskürzeln. | ✅ DONE | 2026-06-20 |
| FEAT-71 | P3 | [FEAT] Accumulation Indicator über Dividenden hinaus (Total Shareholder Yield) | **Kontext (2026-06-19):** Nach FEAT-68 + Umbenennung misst der Indikator bewusst nur *Dividenden*-Akkumulation; Nicht-Dividenden-Compounder (Amazon, viele US-Tech) bekommen „nicht anwendbar". Sie „akkumulieren" aber sehr wohl — über Aktienrückkäufe + Reinvestition zu hohem ROIC. **Idee:** Engine um **Buyback-Yield** (Netto-Reduktion der Aktienzahl) + optional Reinvestitions-/ROIC-Komponente erweitern → Total Shareholder Yield, damit auch buyback-getriebene Compounder fair bewertet werden. **Aufwand:** braucht Fundamentaldaten (Shares-Outstanding-Historie/Buybacks, ROIC) — gleiche Datenquelle wie FEAT-68 B2, daher B2-Scale, P3. **Verwandt:** FEAT-67/68 (Indikator), FEAT-70 (Ground-Truth-Daten). **Anzeige-Entscheidung (2026-06-20, mit User):** *kombiniert*, nicht separat — Buyback fließt in dieselbe Engine-Achse (= eine Matrix-Spalte, kein Bloat), im Position Dashboard aufgefächert in Dividende/Buyback/TSY-Zeilen (FEAT-68-Transparenzprinzip). Eine separate Spalte wäre redundant (Allianz macht beides → TSY summiert). **Umgesetzt (2026-06-20):** `core/shareholder_yield.py` — `buyback_yield(ticker)` via yfinance `get_shares_full` (Netto-Aktienzahl-Delta über ~14M, positiv=Rückkäufe/negativ=Verwässerung, fail-safe→None bei Lücken); `buyback_yield_map` (ThreadPool, parallel) + `cached_buyback_yield_map` (`@st.cache_data` 1 Tag). `compute_accumulation` nimmt jetzt `buyback_pct` → Engine = TSY (Dividende+Buyback), n/a-Guard auf TSY statt nur Dividende → buyback-only-Compounder (AZO, AAPL) werden bewertbar statt „nicht anwendbar". `AccumulationResult.engine_parts` (Dividende/Buyback-Infozeilen ohne eigene Ampel) für die Dashboard-Aufschlüsselung. Engine-Label dynamisch (`comp_tsy` bei Buyback-Daten, sonst `comp_engine`). Umbenennung „Dividenden-Akkumulation"→„Akkumulation" (Section/Col/Help/n/a-Texte). 3 Pages verdrahtet (portfolio_story, watchlist_checker, position_dashboard). i18n de/en. 22 neue Tests (TSY-Summe, buyback-only, engine_parts, Dilution, graceful None + Fetch-Modul gemockt). Verifiziert mit Live-Daten: ALV.DE 4,5%+1,7%=6,2% 🟢 akkumulieren; AAPL 0,4%+2,2%=2,6% 🟢 (vorher „halten"); AZO 2,4% buyback-only → prüfen (vorher n/a). 1293 grün. **Optional offen (P3):** ROIC-/Reinvestitionskomponente (braucht FEAT-68-B2-Fundamentaldaten). | ✅ DONE | 2026-06-20 |
| FEAT-72 | P2 | [IMPR] Vermögenshistorie Tagesveränderung auf EOD-Basis (statt Snapshot-Diff) | **Kontext (2026-06-19):** Snapshot-Diff (heute.total_eur − gestern.total_eur) vergleicht zwei **intraday**-Momentaufnahmen → weicht von der Analyse-Seite (Close-to-Close) ab. Ursache: Snapshots werden tagsüber geschossen und nutzen `current_prices.price_eur` (live). `historical_prices` hat dagegen zuverlässige EOD-Closes. **Idee (einfachste Lösung laut User):** Tagesveränderung in der Vermögenshistorie-Anzeige als `heutiger_snapshot − gestriger_EOD_Wert` berechnen, wobei gestriger EOD-Wert = Σ(Stückzahlen aus gestrigem Snapshot.holdings × historical_prices[symbol][gestern]). Alternativ: Snapshot-Build nach Marktschluss auf EOD-Kurse umstellen (backfill). Ziel: Vermögenshistorie und Analyse-Seite stimmen überein. **Verwandt:** GC=F-Bugfix 2026-06-19 (market_data_agent.py get_prev_close). **Umgesetzt (2026-06-20):** `compute_eod_day_change()` in WealthSnapshotAgent + 4. Metric in wealth_history.py. GC=F-Bugfix committed. 1263 Tests grün. | ✅ DONE | 2026-06-20 |
| FEAT-73 | P2 | [FEAT] Portfolio vs. Benchmark — zeitgewichtete Rendite (TWR) auf Holdings-Basis | **Kontext (2026-06-20, Design-Session mit User):** Es fehlt die Kernfrage des Langfristanlegers — „schlägt mein **Depot** den Index?" Bewusst **Portfolioebene, kein Pro-Positions-Vergleich** (User: „das pro Position brauchen wir nicht"). **Warum nicht der naive Weg:** `total_eur`-Reihe direkt vs. Index ist durch Ein-/Auszahlungen **kontaminiert** — frisches Kapital bläht den Wert auf, unabhängig von Performance → „Depot +20 % vs. Index +8 %" ist wertlos, wenn die Hälfte Einzahlungen sind. **Warum nicht Cashflow-Rekonstruktion/Modified-Dietz:** Stückzahl-Deltas sind zwar ein Cashflow-Signal, aber **reinvestierte Dividenden** würden doppelt zählen (Konto ist holdings-only, kein Cash-Ledger → eine wiederangelegte Dividende manifestiert sich *nur* als Stückzahl-Zuwachs; behandelt man jeden Zuwachs als externen Cashflow, subtrahiert man genau den Dividendenertrag heraus → die Anteils-Ratsche/FEAT-67, also Eriks Kernmechanismus, würde weggerechnet). **Lösung (korrekt, cashflow- UND dividenden-doppelzähl-immun): verkettete TWR auf Holdings-Basis.** `r_i = Σ(Stückzahl_start × Kurs_ende + Dividende) / Σ(Stückzahl_start × Kurs_start) − 1`, dann `TWR = Π(1+r_i) − 1`. Clou: innerhalb einer Teilperiode **feste Stückzahlen** → Return ist reine Kursbewegung (+ Dividende im Zähler); Stückzahl-Änderungen (egal ob Frischkapital ODER Reinvest) passieren nur an **Periodengrenzen** und tauchen in *keiner* Return-Formel auf — sie re-basen nur den Korb. Dadurch existiert das Cashflow-Problem strukturell nicht und die Doppelung kann nicht entstehen. Index (EUNL.DE) durch dieselbe Verkettung → Apples-to-Apples. **Daten liegen:** Snapshot-`holdings` (FEAT-63–66) tragen pro Tag/Titel `quantity`+`price_eur`+`annual_dividend_eur`; `historical_prices`; Benchmark-Symbol-Config (`hindsight_benchmark_symbol`, Default EUNL.DE) + On-Demand-Backfill `fetch_historical_for_symbol` (FEAT-59 v2). **Kritische Datenrealität:** holdings-tragende Snapshots gibt es erst seit **2026-06-17** (4 von 58) → **Forward-Only**, Kurve baut sich über ~2–3 Monate auf (graceful „baut sich auf"-Hinweis bis genug Teilperioden, etabliertes Muster wie FEAT-67/68). Rückwirkend **unmöglich**: vor 06-17 keine Zusammensetzung + `positions` aggregiert (kein Transaktions-Journal) → historische Cashflows nicht rekonstruierbar. **Anzeige:** Vermögenshistorie-Seite — (1) Benchmark-Linie als Overlay auf dem bestehenden Vermögenschart (Toggle-Muster wie Breakdown/Mix/Konzentration), auf Startwert normiert; (2) Metric „Depot vs. Index (zeitgewichtet)". **Beigabe (separat, sofort zeigbar, cashflow-robust):** Drawdown + annualisierte Volatilität aus `total_eur` (58 Snapshots) — leicht verzerrt (Einzahlung kaschiert Drawdown), als solche labeln; optional Underwater-Chart. **Bewusst draußen:** Pro-Positions-vs-Index, geldgewichtete Rendite, jede Vor-Juni-Aussage. **Verwandt:** FEAT-59 (Hindsight-Benchmark-Infra), FEAT-63–66 (Holdings-Snapshots), `core/composition_drift.py` (Forward-Only-Serien-Muster). **Optional Übergang (vom User abgelehnt zugunsten A):** `total_eur` vs. Index als „indikativ/verzerrt"-Overlay. **Umgesetzt (2026-06-20):** `core/portfolio_twr.py` (pure, deterministisch) — `portfolio_twr_series` (verkettete TWR aus Snapshot-`holdings`; Preise kommen aus den Holdings selbst → selbstkonsistent, `price_at`-Fallback nur für zwischen zwei Snapshots komplett verkaufte Titel; Dividende prorata aus `annual_dividend_eur`), `benchmark_twr_series` (gleiche Verkettung auf Index-Level, an Snapshot-Daten ausgerichtet), `drawdown_series` + `volatility_annualized` (beide aus dem cashflow-immunen TWR-Index, nicht aus `total_eur`; Vola erst ab 20 Punkten → „baut sich auf"). Neue Sektion „Rendite vs. Benchmark (zeitgewichtet)" in `pages/wealth_history.py` (Section 1): 4 Metrics (Depot-TWR / Index / Überrendite / Vola), Zwei-Linien-Chart (Depot vs. Index gestrichelt), optionaler Underwater-Drawdown-Chart, Benchmark-Symbol-Feld (teilt `hindsight_benchmark_symbol`-Config + Backfill-Button mit Verdict Hindsight). i18n de/en (18 `twr_*`-Keys, Parität geprüft). 15 neue Tests (Kernfall „Stückzahl-Zuwachs ≠ Return", Verkettung, Wertgewichtung, Dividende, verkaufter Titel, Benchmark-Anker, Drawdown, Vola-Schwelle). Live verifiziert: 4 Holdings-Snapshots → Depot +0,41 % seit 17.06.; EUNL.DE-Linie vorerst flach (Index-Historie noch nicht frisch — Frische-Caveat, kein Bug, identisch zu Hindsight). 1308 grün. **Beigabe Drawdown/Vola sind ebenfalls forward-only umgesetzt (aus TWR-Index statt `total_eur` → methodisch konsistent, keine Cashflow-Verzerrung).** **Folge-Polish (2026-06-20, User-Feedback):** (1) X-Achsen beider Charts auf `%d.%m.%Y` (tagesbasiert, keine Uhrzeit) inkl. Hover. (2) Benchmark-Frische: statt nur „Historie vorhanden?" jetzt Stale-Check (`latest_bench_date < twr_start`) → flache 0%-Linie vermieden, neuer `twr_bench_stale`-Hinweis + Laden-Button füllt den Index bis zum Startdatum auf. **Nachgezogen (2026-06-20):** Benchmark ist jetzt Teil des täglichen Refresh — `MarketDataAgent` bekam optionalen `app_config_repo` + `_benchmark_symbols()`, `fetch_all_now(fetch_history=True)` zieht den konfigurierten Index (nur Historie, nicht als Position in der Kursabfrage) automatisch mit; Key/Default zentralisiert in `core/constants.py` (`BENCHMARK_SYMBOL_KEY`/`DEFAULT_BENCHMARK_SYMBOL`), beide Pages (Hindsight + Vermögenshistorie) nutzen die Konstanten. Manueller Laden-Button bleibt als Fallback. EUNL.DE einmalig backfilled (bis 19.06.) → Linie rendert sofort. (3) **Non-Börsentag-Fetch-Guard:** neue pure `core/trading_calendar.py` (`is_trading_day`/`last_trading_day`, wochenend-aware, Feiertage bewusst nicht modelliert); Auto-Fetch in `pages/analyse.py` feuert an Handelstagen weiter intraday (>1h), an Nicht-Handelstagen nur wenn der letzte Session-Close noch nicht erfasst ist (kein sinnloser Wochenend-Fetch, aber Freitags-Close wird nicht verpasst). 5 neue Tests. 1313 grün. | ✅ DONE | 2026-06-20 |
| FEAT-74 | P3 | [FEAT] Accumulation Candidate Scanner — Discovery-Skill für SearchAgent | **Kontext (2026-06-21):** Gegenstück zum deterministischen Accumulation Indicator (FEAT-68/71): Der Indikator *bewertet* vorhandene Positionen, aber es fehlte der **Zufluss** — ein Skill, der aktiv neue Kandidaten mit dem gleichen Profil *findet*. **Lösung:** neuer SearchAgent-Skill (Cloud ☁️, kein neuer Agent/Code, reine `config/default_skills.yaml`-Ergänzung in der `search`-Area) der das Indikator-Profil spiegelt: screent auf alle drei Faktoren **gemeinsam** — (1) Income-Engine = **Total Shareholder Yield** (Dividende + **netto** Buyback, Schwellen 🟢≥2,5%/🟡≥1,2%/🔴, Split sichtbar — explizit auch No-/Low-Dividend-Buyback-Compounder), (2) These intakt (Survival/Moat/Bilanz), (3) Bewertung nicht teuer. Universe: DM-Equities (US/Europa/optional Japan), Mid/Large-Cap >€1bn, keine Fonds/ETFs. Output pro Kandidat mit TSY-Breakdown + Accumulation-Read (`akkumulieren`/`beobachten`/`kein_fit`). **Designprinzip (kritisch):** bewusst **Discovery-Funnel, kein Verdict** — Web-Zahlen können unpräzise/konfabuliert sein → Skill schlägt nur Kandidaten an die Watchlist vor, der **deterministische App-Indikator** rekonfirmiert sie aus Ground-Truth (Valuation-Yield + Buyback-Map + SC/FA). Spiegelt [[accumulation_indicator_as_diagnostic]]. **Verwandt:** FEAT-67/68/71. **Umgesetzt (2026-06-21):** Skill in `config/default_skills.yaml` (`search`-Area). Probesuche verifiziert (ING ✓, TotalEnergies: These solide aber Bewertung nicht grün → `beobachten`, nicht `akkumulieren`). Config-only, 21 Skill-Tests grün. Commit 16e3a09. | ✅ DONE | 2026-06-21 |

### Technical Debt

| ID | Priority | Description | Notes |
|---|---|---|---|
| DEBT-8 | P3 | Document `migrate_db()` inline — add comments explaining the dual init+migrate pattern | Low risk, cosmetic |
| DEBT-13 | P3 | Tighten requirements.txt version bounds | Low urgency, no known conflicts |

---
